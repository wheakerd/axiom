interface:
  display_name: "Use Axiom"
  short_description: "Route installed Axiom skills from a session bootstrap"
  default_prompt: "Use $using-axiom to decide whether Axiom has a matching skill for this request."
policy:
  allow_implicit_invocation: false
