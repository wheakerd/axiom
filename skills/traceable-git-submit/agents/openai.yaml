interface:
  display_name: "Traceable Git Submit"
  short_description: "Trace checkpoints and explicit Git submissions"
  default_prompt: "Use $traceable-git-submit to checkpoint, consolidate, recover, or submit and verify this Git work without inferring extra authority."
