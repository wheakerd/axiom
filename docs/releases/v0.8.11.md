Version `0.8.11` is the patch release for [Issue #61](https://github.com/wheakerd/axiom/issues/61). It restores durable headroom in the always-loaded `using-axiom` routing gate without changing its public routes, authorization boundary, or fixed routing workload.

## Durable Routing Headroom

The gate now keeps route-selection facts in the startup surface while leaving ordinary Git staging, conflict, target, and retry mechanics with `traceable-git-submit`'s directly routed `references/direct-submit.md` owner. The six exact `effective-instructions` mode triggers remain explicit in a shorter `agents-architect` entry.

| Metric | v0.8.10 before | v0.8.11 after | Delta |
| --- | ---: | ---: | ---: |
| UTF-8 bytes | 7,739 | 6,673 | -1,066 |
| Words | 1,001 | 852 | -149 |
| Logical lines | 135 | 120 | -15 |
| Direct references | 1 | 1 | 0 |
| Estimated tokens | 1,935 | 1,669 | -266 |
| 8,192-byte headroom | 453 (5.53%) | 1,519 (18.54%) | +1,066 |

The 67-case fixed workload remains byte-identical: 47 routed cases and 20 no-route controls across 11 files. Equivalent static contract validation passes before and after the reduction. These exact repository counts are context proxies; the token values are reproducible `ceil(UTF-8 bytes / 4)` estimates for the same English Markdown surface, not host token or credit totals.

## Reduction Evidence Contract

The context-budget validator now binds a reduction experiment to the immediate predecessor's candidate surface instead of confusing the immutable v0.7.9 cumulative baseline with the actual before state. The v0.7.9 gate remains the cumulative growth baseline, while the paired experiment binds v0.8.10 SHA-256 `7da583bb99880157a7fbca539ebb0426f49cd79d2748be51e5ef5fe85eaa3996` to the v0.8.11 candidate SHA-256 `e6b17e3b23669a174c309752058b1c1ed7c2074c5730e72b51618bbc69eddda2` over the same workload identity.

Contributors now have a documented engineering target: preserve at least 15% headroom below the 8,192-byte instruction boundary, with roughly 6-6.5 KiB preferred when routing and safety acceptance remains equivalent. The hard boundary remains a rejection guard rather than an authoring target.

## Local Static Validation

The complete 110-test standard-library suite, focused 44-test routing run, aggregate publication guard, distribution drift guard, compatibility self-test, routing-context budget check, canonical-facts check, JSON parsing, English-only scan, instruction-size check, skill quick validation, and whitespace checks pass locally on the candidate tree. These checks are static and local; they do not claim pull-request checks, protected-main execution, installed Codex host observation, or published-release success.

The bundled local Codex plugin validator retains its documented stale-schema rejection of the unchanged supported `hooks`, `brandColorDark`, and `supportURL` fields. Claude Code strict offline package validation passes in a disposable copy; authenticated Claude Code host and lifecycle validation is unavailable and remains `UNAVAILABLE / NOT-RUN`.

## Exact Draft Evidence Validation

The `Publish immutable release` contract requires one future draft named `Axiom v0.8.11` at the exact GitHub-signed `main` commit. It requires one content-addressed 17-case Codex observation, binds its filename, digest, size, version, tag, commit, tree, response schema, ordered cases, and zero-regression gates, emits and revalidates one deterministic attestation, and only then publishes the exact draft as immutable and GitHub Latest.

No release pull request, protected-branch check, signed merge, `v0.8.11` tag, external 17-case observation, draft Release, publication workflow, immutable Release, GitHub Latest transition, signature guard, or Issue closure is claimed by the checked-in candidate.

## Evidence Boundary

The checked-in release status remains `STATIC-ONLY`. Codex host and lifecycle observation for this routing change is `NOT-RUN`; equivalent repository-static before/after validation is not relabeled as host behavior. The immutable v0.8.10 release and every prior observation remain separate history.

## Routing Context Facts

<!-- release-facts:v0.8.11-context-budget:start -->
The [v0.8.11 routing-context record](../../evals/context-budget/results/v0.8.11.json) uses the immutable v0.7.9 `using-axiom` gate as its cumulative baseline. The baseline has 5,899 UTF-8 bytes, 757 whitespace-delimited words, 107 logical lines, 1 direct reference, and an estimated 1,475 tokens. The candidate has 6,673 UTF-8 bytes, 852 whitespace-delimited words, 120 logical lines, 1 direct reference, and an estimated 1,669 tokens. Its cumulative deltas are +774 bytes, +95 words, +13 lines, 0 references, and +194 estimated tokens. The record marks the absolute threshold `reached`, the relative threshold `reached`, and review status `reviewed`. The exact static counts are context proxies, and each `ceil(UTF-8 bytes / 4)` figure is only an estimate for the same English Markdown surface, not an exact token or credit count. Codex host and lifecycle observation remains `NOT-RUN`; authenticated Claude Code remains `UNAVAILABLE / NOT-RUN`. No host observation is inferred from these static values.
<!-- release-facts:v0.8.11-context-budget:end -->
