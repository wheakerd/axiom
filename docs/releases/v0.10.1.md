Candidate prepared: 2026-09-10

Version `0.10.1` is a compatible correction for [Issue #117](https://github.com/wheakerd/axiom/issues/117). Six canonical Skill descriptions make existing compound-request ordering and mutually exclusive architecture or installation boundaries visible during native discovery. They add no route, permission or case-specific answer.

## Runtime And Migration

Both plugin manifests advance together. Runtime-contract schema v1 retains 61 installed inputs with digest `sha256:007f0fbe99e6f3aa0b0ce35d879a75e42dc1f88882a3219aaae83d77d0d149c2`. Repository policy revision 8 records the new source candidate. Hooks and their wrappers remain unchanged. The ordinary no-Hook builder must use this committed canonical source and recalculate its own profile, manifest and archive identities before a new observation batch.

The original routing and no-Hook case requests, IDs, classifications, expected routes and scorer are unchanged. The v0.10.0 context record, revision records 1-7, immutable tag history and prior host results retain their original identities and PASS, FAIL, INCOMPLETE or NOT-RUN outcomes. None becomes evidence for the new runtime.

## Evidence Boundary

The always-loaded routing gate is 7,110 UTF-8 bytes, with 1,082 bytes of headroom below the 8 KiB instruction boundary. Its exact static counts and token estimate are context proxies, not observed host usage. The current full-profile Codex host and lifecycle observation is NOT-RUN; authenticated Claude Code observation remains UNAVAILABLE / NOT-RUN. No new native acceptance is inferred from description changes or historical CI.

## Exact Draft Evidence Validation

This is an unreleased candidate with `pending-immutable-tag` binding. Local validation, any subsequent implementation commit, new bundle construction and host observation must each retain their actual identities and outcomes. This note creates no tag, GitHub Release, listing or marketplace publication, and does not close Issue #117. Required user action for existing installations: None.

## Routing Context Facts

<!-- release-facts:v0.10.1-context-budget:start -->
The [v0.10.1 routing-context record](../../evals/context-budget/results/v0.10.1.json) uses the immutable v0.7.9 `using-axiom` gate as its cumulative baseline. The baseline has 5,899 UTF-8 bytes, 757 whitespace-delimited words, 107 logical lines, 1 direct reference, and an estimated 1,475 tokens. The candidate has 7,110 UTF-8 bytes, 891 whitespace-delimited words, 124 logical lines, 2 direct references, and an estimated 1,778 tokens. Its cumulative deltas are +1,211 bytes, +134 words, +17 lines, +1 reference, and +303 estimated tokens. The record marks the absolute threshold `reached`, the relative threshold `reached`, and review status `reviewed`. The exact static counts are context proxies, and each `ceil(UTF-8 bytes / 4)` figure is only an estimate for the same English Markdown surface, not an exact token or credit count. Codex host and lifecycle observation remains `NOT-RUN`; authenticated Claude Code remains `UNAVAILABLE / NOT-RUN`. No host observation is inferred from these static values.
<!-- release-facts:v0.10.1-context-budget:end -->
