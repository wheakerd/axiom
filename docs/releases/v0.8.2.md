# Axiom v0.8.2

Status: unreleased repair candidate

Version `0.8.2` is a compatible patch for the Stage 3 routing defect tracked by
[Issue #34](https://github.com/wheakerd/axiom/issues/34). It clarifies that
publishing an already-prepared artifact selects only
`confirm-external-action`; publication alone is not a persistent system
change. A distinct installation, deployment, migration, activation, or
retention change still selects `reversible-system-change`, including dual
routing when it independently carries an external effect.

Both manifests are synchronized at `0.8.2`. Route names, descriptions, order,
authorization, hooks, marketplace wrappers, corpus, benchmarks, and historical
observations remain unchanged. This checked-in candidate creates no tag,
Release, pull request, Issue mutation, asset upload, or other remote action.

## Preserved And Candidate Evidence

The F4 Codex diagnostic against immutable v0.8.1 remains terminal `FAIL`. Its
first seven calls passed; Call 8, `near-miss-confirm-plugin-publish-001`,
incorrectly selected `confirm-external-action` and
`reversible-system-change`. The batch stopped after eight calls, made no retry,
and did not run the remaining 11 planned calls. F4 is not reclassified.

After the 143-byte wording fix, F5 ran 19 planned Codex CLI `0.149.1` calls
with `gpt-5.4`: three Case 1 variance samples, then Cases 2-17 once each. All
19 passed with zero tool events and unchanged workspace, source, and installed
snapshots. Aggregate usage was 279,939 input tokens, 156,288 cached input
tokens, 2,436 output tokens, and 350,053 milliseconds.

F5 truthfully identifies version `0.8.1`, a null tag, commit
`298268ac0cfcaac84af22d7117e126f57e72152c`, tree
`ea298f5a81ca59eeecee863743b714f9f97f201d`, and release state
`candidate-unreleased`. Its tested base-plus-diff identity maps exactly to that
commit and tree. The three variance samples mean F5 is not a schema-v2 17-call
release observation, and it is not final v0.8.2 evidence.

The later release-bound batch against signed v0.8.2 candidate commit
`b4e4eb4a1341f35155d8916e53ba1715015cfd7d` remains terminal `FAIL` at
Case 1 after unexpected tool use. It made one call, made no retry, and left
Cases 2-17 `NOT-RUN`. One independent corrected-preflight Case 1 diagnostic
against that subject then passed at repeat count one with the expected route,
zero clarification, false model mutation fields, zero tool events or calls,
and unchanged protected snapshots. The two one-sample outcomes are variance,
not Stage 3 acceptance.

Signed commit `ca9ae1803a806042866b0c4d288791f0f32df8f1` was a separate unreleased
v0.8.3 successor attempt. Both later complete-batch attempts stopped at Case 1
as immutable external terminal `UNKNOWN` records. They are not copied into this
repository, reclassified, or treated as host passes; no v0.8.2 or v0.8.3 tag
or Release was created.

The next signed v0.8.2 repair commit,
`9dbc2592dc2e544d3f62aafb2788af7efc503840`, received one new release-bound
batch. Case 1 returned the expected route and valid V3 response, false mutation
fields, zero tool events or calls, and unchanged source, installed-plugin, and
workspace snapshots. The old hook-trust bypass necessarily added a startup
`ConfigWarning`; the observer correctly classified the resulting public error
item as terminal. The batch stopped `FAIL` after that one call without retry,
and Cases 2-17 remain `NOT-RUN`.

## Observer Repair

The versioned [Codex exec JSONL taxonomy](../../evals/codex-exec-jsonl-observer-v2.json)
binds the closed eight top-level and nine item discriminators emitted by Codex
CLI `0.149.1` to exact official `openai/codex` source. The existing routing
validator classifies each item before lifecycle sequencing. Known benign items
may occur after `thread.started` and before `turn.started`; tool/action and
error items are counted and terminate even in that position. Unknown,
malformed, invalid-status, pre-thread benign, duplicate-phase, post-terminal,
and abrupt streams fail closed.

The classifier is standard-library validation code, not a model runner. Its
sanitized journal permits only public discriminators, fixed categories and
roles, enumerated statuses, and ordinals. It never retains response or
reasoning text, identifiers, tool arguments or output, credentials, paths,
session/config content, or raw payload. The prior terminal outcomes remain
unchanged because their destroyed private subtypes cannot be reconstructed.

The Phase 6 diagnosis binds Codex CLI `0.149.1` source directly: the temporary
PATH-alias line and stdin diagnostic are direct stderr and cannot create a
JSONL item. The hook-trust bypass instead contributes one startup
`ConfigWarning`, and `codex exec --json` exposes each such warning as an error
item. The repaired setup prevents that producer rather than weakening the
taxonomy: it places the disposable `CODEX_HOME` in an owner-only runtime root
outside system temporary storage, removes the bypass, and trusts only the
byte-verified installed hook through native `hooks/list`,
`config/batchWrite`, and repeated `hooks/list` verification. The trust state is
disposable and authentication is unavailable to the zero-model exchange.

## Context Review

The always-loaded gate is 6,293 UTF-8 bytes, 805 whitespace-delimited words,
114 logical lines, one unique direct reference, and an estimated 1,574 tokens.
Its SHA-256 is
`5e852f5cc1edc3a2fdb19538b2e8bec982d9e959012cb426ae91bb96ccfef866`.
Against immutable v0.7.9, the cumulative delta is +394 bytes, +48 words, +7
lines, zero references, and +99 estimated comparison tokens. Both the 256-byte
and 5% review thresholds are reached. The reviewed justification is the narrow
publication-only boundary; no safety rule was removed to manufacture a lower
count.

F5 usage is stored as observed candidate metrics only. It is not v0.8.2 host,
tag, release, or lifecycle evidence. Case 17 was a fresh-session routing
observation; actual post-compaction behavior remains `NOT-RUN`. Authenticated
Claude Code host and lifecycle behavior remain `UNAVAILABLE / NOT-RUN` because
no subscription or authenticated session is available.

## Final Stage 3 Gate

The checked-in release status remains `STATIC-ONLY` with a null commit. After
this hook-trust repair is merged as one GitHub-signed commit, run the complete
17-case batch against that exact commit before creating a tag. If it fails,
stop without minting `v0.8.2`. If it passes, create immutable tag `v0.8.2` at
the same commit, finalize the sanitized schema-v2 record with that exact tag,
commit, and tree, and validate it with the explicit external mode documented in
[Field Validation](../field-validation.md#post-merge-routing-observation).

The record filename must expose its full SHA-256. Upload that exact record as a
release asset and include the full digest in the Release and Issue text. The
asset supplements but never promotes or rewrites checked-in `STATIC-ONLY`
status. Close Issue #34 only after the asset and release checks succeed.

## Candidate Validation

Validation covers the complete standard-library suite, aggregate publication
policy, external-mode positive and adversarial cases, context reproduction,
distribution drift, compatibility evidence, duplicate-aware JSON and JSONL,
Markdown links and fragments, ASCII, instruction sizes and references,
manifest and hook parity, expected paths and modes, historical-byte
preservation, exact-version JSONL taxonomy and adversarial classifier, Skill
validation, package validation, and Claude Code strict offline validation when
the existing CLI remains available.

Authenticated Claude Code routing, actual post-compaction lifecycle,
active-user installation, marketplace or portal submission, final post-merge
Codex observation, tag creation, asset upload, Release publication, and Issue
closure are not performed by this checked-in candidate.

For the complete boundary, see [Compatibility](../compatibility.md). For the
version history, see [the changelog](../../CHANGELOG.md).
