Version `0.8.16` is the repository-CI patch for [Issue #84](https://github.com/wheakerd/axiom/issues/84). It adds deterministic cross-platform runtime evidence for the checked-in startup commands without changing installed Skills, hooks, routes, action authority, benchmark membership, or runtime dependencies.

## Cross-Platform Hook Runtime Contract

The offline harness executes each exact checked-in Codex `SessionStart` command and the applicable Claude Code command rather than reimplementing their behavior. Its read-only GitHub Actions matrix uses native `ubuntu-24.04`, `windows-2025`, and `macos-15` runners, full immutable Action SHAs, explicit Python and Node.js versions, and actual Python, Node.js, Git, Bash, platform, and architecture reporting.

The runtime cases cover plugin and workspace paths containing spaces, shell quoting and environment expansion, exact Skill bytes, explicit UTF-8 and newline behavior, missing files, nonzero exits, independent stdout and stderr, and bounded timeout cleanup. Structural workflow and manifest checks remain separate from runtime execution.

## Installed Surface And Contributor Boundary

This is repository-only CI evidence. The installed Codex and Claude Code hook JSON, wrappers, Skills, routes, and action authority are unchanged from v0.8.15. The matrix is a separate read-only workflow and does not weaken or rename the existing exact unit workflow contract.

Current ruleset evidence keeps `repository-guards` and `unit-and-integration-tests` as the required identities. The three new `hook-runtime-ubuntu-24.04`, `hook-runtime-windows-2025`, and `hook-runtime-macos-15` checks remain non-required while native CI stability is observed, so contributor branch and fork paths are not newly blocked.

## Direct Local Validation

The repository runtime suite passes 5/5 directly on Fedora Linux with kernel 7.1.8, Python 3.14.7, Node.js 24.18.0, Git 2.55.0, and Bash 5.3.9. The complete standard-library suite passes 128 tests with one expected native-Windows-only skip. Publication, distribution-drift, compatibility, routing-context, canonical-facts, JSON, English-only, changed-file whitespace, and Claude Code 2.1.220 strict offline package validation also pass in a disposable copy.

The bundled local Codex `plugin-creator` retains its documented known validator conflict: it rejects Axiom's supported `hooks`, `interface.brandColorDark`, and `interface.supportURL` fields. That non-pass remains explicit and is not used to remove installed hook or marketplace metadata.

## Exact Draft Evidence Validation

The GitHub-hosted `ubuntu-24.04`, native `windows-2025`, and native `macos-15` workflow jobs are `NOT-RUN` in this local preparation. In particular, no native Windows or macOS pass is inferred from Fedora Linux. Installed Codex host and lifecycle observation is `NOT-RUN`; authenticated Claude Code installed-host and lifecycle observation is `UNAVAILABLE / NOT-RUN` until external CI or host evidence exists.

No release pull request, merge, `v0.8.16` tag, draft Release, publication workflow, immutable Release, GitHub Latest transition, Issue closure, required-check change, or native GitHub Actions completion is claimed by this repository-only candidate. The immutable v0.8.15 release and every prior host or process-boundary observation remain separate history.

## Routing Context Facts

<!-- release-facts:v0.8.16-context-budget:start -->
The [v0.8.16 routing-context record](../../evals/context-budget/results/v0.8.16.json) uses the immutable v0.7.9 `using-axiom` gate as its cumulative baseline. The baseline has 5,899 UTF-8 bytes, 757 whitespace-delimited words, 107 logical lines, 1 direct reference, and an estimated 1,475 tokens. The candidate has 6,960 UTF-8 bytes, 894 whitespace-delimited words, 124 logical lines, 1 direct reference, and an estimated 1,740 tokens. Its cumulative deltas are +1,061 bytes, +137 words, +17 lines, 0 references, and +265 estimated tokens. The record marks the absolute threshold `reached`, the relative threshold `reached`, and review status `reviewed`. The exact static counts are context proxies, and each `ceil(UTF-8 bytes / 4)` figure is only an estimate for the same English Markdown surface, not an exact token or credit count. Codex host and lifecycle observation remains `NOT-RUN`; authenticated Claude Code remains `UNAVAILABLE / NOT-RUN`. No host observation is inferred from these static values.
<!-- release-facts:v0.8.16-context-budget:end -->
